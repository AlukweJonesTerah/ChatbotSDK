// install phpmyadmin

1. sudo apt install mysql-server
2. sudo apt-get install apache2
3. sudo systemctl status mysql
4. sudo systemctl status apache2
5. sudo apt install php
6. sudo phpenmod mbstring
7. sudo systemctl restart apache2
8. sudo apt install net-tools
9. ifconfig

// Creating the database and table(s)

1. sudo mysql
2. show databases;
3. create database mydatabase;
4. use mydatabase;
5. create table users(
    id int,
    name varchar(100),
    email varchar(100),
    password varchar(200)
);

libmysqlclient-dev - nstall the MySQL development packages - on Ubuntu/Debian
MYSQLCLIENT_CFLAGS - environment variables to help pip find the MySQL libraries 
MYSQLCLIENT_LDFLAGS
MarkupSafe==2.1.5
pip install -r requirements.txt

// for flask_mysqldb import MySQL error (module not found)
pip install --force-reinstall flask-mysqldb -  restart IDE 

// for flask_mysqldb import MySQL error (module not found still )
// use

from flaskext.mysql import MySQL

// checking install pips

source .venv/bin/activate
pip list

// running xampp 

sudo ./xampp-linux-x64-8.0.30-0-installer.run
sudo /opt/lampp/manager-linux-x64.run
or
chmod u+x located directory/xampp-linux-x64-8.0.30-0-installer.run
sudo located directory/xampp-linux-x64-8.0.30-0-installer.run


py -3 -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
flask run